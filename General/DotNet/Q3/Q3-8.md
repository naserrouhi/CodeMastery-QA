# Q3-8: What is DI container?

- It is a tool (a library/framework feature) that:
  
  - Automatically creates objects for you (including their dependencies).
  - Resolves dependencies based on the types you've registered.
  - Manages lifetimes (singleton, scoped, transient).
  - Reduces boilerplate code for wiring things together.
